/// <reference types="vite/client" />

// Compiled in by vite.config.ts. ⚠ __APP_BUILD__ is the EMPTY STRING on the dev
// server, on purpose — render the build only when it is non-empty.
declare const __APP_BUILD__: string
declare const __APP_VERSION__: string
